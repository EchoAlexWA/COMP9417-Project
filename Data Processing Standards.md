1. 使用pandas DataFrame作为数据存储格式，可使用pickle库进行序列化存储，方便后续加载。
2. 数据中不存在nan值，无需额外处理。
3. 所有列数据类型均为float64，且行数一致，符合预期，无需转换。
4. 数据保存在'data/'目录下。
5. 应该保存带有异常值和不带有异常值的两个版本，方便后续对比分析。
6. 多整理在数据处理过程中的关键步骤、参数设置、假设和中间结果，可阶段性进行小结，方便后续撰写报告。